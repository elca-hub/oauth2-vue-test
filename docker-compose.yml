version: '3.6'
services:
  web:
    build:
      context: web
    ports:
      - "8080:8080"
  web-test:
    image: nginx:1.21.6
    volumes:
      - ./web/dist:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8080:80"